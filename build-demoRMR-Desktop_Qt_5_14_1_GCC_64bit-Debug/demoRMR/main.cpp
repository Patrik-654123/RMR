#include "mainwindow.h"
#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
    w.show();
  ///git coments
  /// 2.zmena
  /// zmena 3
    return a.exec();
}
